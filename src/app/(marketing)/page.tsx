import { LandingPage } from '@/components/marketing/landing-page'

/** Hearst Connect public landing — server-rendered marketing shell. */
export default function HomePage() {
  return <LandingPage />
}
